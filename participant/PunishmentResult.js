import React, { Component } from 'react'
import { connect } from 'react-redux'

import { fetchContents } from './actions'

const mapStateToProps = ({}) => ({
})

const PunishmentResult = ({}) => (
  <div>
    <p>罰結果画面</p>
  </div>
)

export default connect()(PunishmentResult)
