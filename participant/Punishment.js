import React, { Component } from 'react'
import { connect } from 'react-redux'

const mapStateToProps = ({}) => ({
})

const Punishment = ({}) => (
  <div>
    <p>罰画面</p>
  </div>
)

export default connect()(Punishment)
